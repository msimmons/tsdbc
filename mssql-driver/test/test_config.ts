import * as mssql from 'mssql'
import { DriverConfig } from 'tsdbc'

export const config = {
    host: 'localhost',
    username: 'sa',
    password: 'K3nd0n60',
    database: undefined
} as DriverConfig